{% extends 'base.twig' %}
{% block title %}Yukinator{% endblock %}
{% block body %}
    <div class="col-8">
        <div class="row d-flex justify-content-center">
            <div class="col-2 fs-1 text-end">&#129299;</div>
            <div class="col-9">
                <div class="speech-bubble d-flex align-items-center">
                    <p>Très bien, je m'en souviendrai !</p>
                </div>
                <div class="row mt-4">
                    <div class="col-6">
                        <a href="{{ url('game.start') }}" class="btn btn-primary">Rejouer</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
